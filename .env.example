# Lovable / NTOC Budget Management — example env
# The prototype runs without any env vars. Add these when wiring a real backend.

# VITE_API_URL=https://api.example.com
# VITE_AUTH_DOMAIN=auth.example.com
